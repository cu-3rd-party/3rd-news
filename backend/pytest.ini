[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts =
    -ra
    --strict-markers
    --strict-config
    --cov=lib
    --cov=main
    --cov-report=term-missing:skip-covered
    --cov-fail-under=70
