[pytest]
testpaths = tests
# Only the main service goes on the path: all three services name their package
# `app`, so the classifier modules are loaded by file path in conftest.py.
pythonpath = services/main
addopts = -q
